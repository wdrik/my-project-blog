'use strict';

/**
 * raffle-participant router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::raffle-participant.raffle-participant');
